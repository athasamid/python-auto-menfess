class Url(object):
    def __init__(self, display_url, expanded_url, indices, url):
        self.display_url = display_url
        self.expanded_url = expanded_url
        self.indices = indices
        self.url = url
